<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.application.dao.ApplicationDao">

    <!-- 分页查询入校申请列表 -->
    <select id="selectApplicationPage" resultType="io.renren.modules.application.entity.ApplicationEntity">
        SELECT * FROM ers_application
        <where>
            <if test="realName != null and realName != ''">
                AND visitor_name LIKE CONCAT('%', #{realName}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone LIKE CONCAT('%', #{phone}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="startDate != null">
                AND DATE(entry_date) >= DATE(#{startDate})
            </if>
            <if test="endDate != null">
                AND DATE(entry_date) &lt;= DATE(#{endDate})
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 统计待审批申请数量 -->
    <select id="countPendingApplications" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM ers_application WHERE status = 0 AND deleted = 0
    </select>

    <!-- 批量更新申请状态 -->
    <update id="batchUpdateStatus">
        UPDATE ers_application
        SET status = #{status},
            approval_user_id = #{approverId},
            approval_time = NOW(),
            approval_remark = #{remark},
            update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 批量删除申请（仅限未审批和已取消状态） -->
    <delete id="batchDeleteByStatus">
        UPDATE ers_application
        SET deleted = 1, update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND status IN
        <foreach collection="allowedStatus" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
    </delete>

</mapper>
